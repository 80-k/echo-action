import * as core from '@actions/core';

const name = core.getInput('name');
const result = `Hello, ${name}`;
print("inp", name)
print("This main.js is running...")
print("output", to-the-world)
core.setOutput('to-the-world', result);
